packages:
  - "packages/*"
  - "tools/*"
allowBuilds:
  # esbuild compiles a native binary on install, so it must run.
  # @nestjs/core only runs a telemetry postinstall, which we skip.
  '@nestjs/core': false
  esbuild: true
